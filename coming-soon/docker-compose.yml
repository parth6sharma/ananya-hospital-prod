services:
  web:
    build: .
    ports:
      - "85:80"
      - "443:443"
    volumes:
      - certs:/etc/letsencrypt
      - certs-data:/var/www/certbot
    restart: unless-stopped

volumes:
  certs:
  certs-data: